import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:movie_recommend/app/modules/error_movie/error_movie_page.dart';

void main() {
  testWidgets('ErrorMoviePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ErrorMoviePage(title: 'ErrorMovie')));
    //  final titleFinder = find.text('ErrorMovie');
    //  expect(titleFinder, findsOneWidget);
  });
}
