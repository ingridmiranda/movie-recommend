import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:movie_recommend/app/modules/about_us/about_us_page.dart';

void main() {
  testWidgets('AboutUsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AboutUsPage(title: 'AboutUs')));
    //  final titleFinder = find.text('AboutUs');
    //  expect(titleFinder, findsOneWidget);
  });
}
